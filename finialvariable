package inheritance;

public class finialVariable {
	final int speed = 40;// final we can't change the value

	public static void main(String[] args) {
		finialVariable fv = new finialVariable();
		// fv.speed =100; // compiler error
		System.out.println(fv.speed);
	}

}
